apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "primeagent.fullname" . }}
  labels:
    app: {{ template "primeagent.name" . }}
    chart: {{ template "primeagent.chart" . }}
    release: {{ .Release.Name }}
    primeagent-scope: "backend"
spec:
  serviceName: "{{ include "primeagent.fullname" . }}-service"
  replicas: {{ .Values.primeagent.backend.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "primeagent.name" . }}
      chart: {{ template "primeagent.chart" . }}
      release: {{ .Release.Name }}
      primeagent-scope: "backend"
  template:
    metadata:
      {{- with .Values.primeagent.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app: {{ template "primeagent.name" . }}
        chart: {{ template "primeagent.chart" . }}
        release: {{ .Release.Name }}
        primeagent-scope: "backend"
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "primeagent.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      volumes:
        {{- if .Values.secretProvider.enabled }}
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "{{- .Values.secretProvider.name }}"
        {{- end }}
        {{- with .Values.primeagent.backend.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.primeagent.backend.image.repository }}:{{ .Values.primeagent.backend.image.tag  | default .Values.primeagent.global.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.primeagent.backend.image.imagePullPolicy | default .Values.primeagent.global.image.imagePullPolicy }}
          command: ["/bin/bash", "-c"]
          args:
            - >
              set -e &&
              if [ -n "$LF_CHART_EXTERNALDB_DRIVER" ]; then
                echo "LF_CHART_EXTERNALDB_DRIVER is set, using external database at $LF_CHART_EXTERNALDB_HOST" &&
                export PRIMEAGENT_DATABASE_URL="$LF_CHART_EXTERNALDB_DRIVER://$LF_CHART_EXTERNALDB_USER:$LF_CHART_EXTERNALDB_PASSWORD@$LF_CHART_EXTERNALDB_HOST:$LF_CHART_EXTERNALDB_PORT/$LF_CHART_EXTERNALDB_DATABASE"
              else
                echo "LF_CHART_EXTERNALDB_DRIVER is not set, using SQLLite database"
              fi &&
              primeagent run --host 0.0.0.0 --port {{ .Values.primeagent.backend.service.port }}
          ports:
            - name: http
              containerPort: {{ .Values.primeagent.backend.service.port }}
              protocol: TCP
          {{- if or .Values.secretProvider.enabled .Values.primeagent.backend.sqlite.enabled .Values.primeagent.backend.volumeMounts }}
          volumeMounts:
            {{- if .Values.secretProvider.enabled }}
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
            {{- end }}
            {{- if .Values.primeagent.backend.sqlite.enabled }}
            - name: data
              mountPath: /data
            {{- end }}
            {{- with .Values.primeagent.backend.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
              
            initialDelaySeconds: {{ .Values.primeagent.backend.probe.initialDelaySeconds }}
            periodSeconds: {{ .Values.primeagent.backend.probe.periodSeconds }}
            timeoutSeconds: {{ .Values.primeagent.backend.probe.timeoutSeconds }}
            failureThreshold: {{ .Values.primeagent.backend.probe.failureThreshold }}   
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.primeagent.backend.probe.initialDelaySeconds }}
            periodSeconds: {{ .Values.primeagent.backend.probe.periodSeconds }}
            timeoutSeconds: {{ .Values.primeagent.backend.probe.timeoutSeconds }}
            failureThreshold: {{ .Values.primeagent.backend.probe.failureThreshold }}   
          env:
            - name: PRIMEAGENT_BACKEND_ONLY
              value: "{{ .Values.primeagent.backend.backendOnly }}"
            - name: PRIMEAGENT_NUM_WORKERS
              value: "{{ .Values.primeagent.backend.numWorkers }}"
            {{- if .Values.primeagent.backend.sqlite.enabled }}
            - name: PRIMEAGENT_DATABASE_URL
              value: "sqlite:////data/primeagent.db"
            {{- end }}
            {{- with .Values.primeagent.backend.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.primeagent.backend.externalDatabase.enabled }}
            - name: LF_CHART_EXTERNALDB_DRIVER
            {{- toYaml .Values.primeagent.backend.externalDatabase.driver | nindent 14 }}
            - name: LF_CHART_EXTERNALDB_HOST
            {{- toYaml .Values.primeagent.backend.externalDatabase.host | nindent 14 }}
            - name: LF_CHART_EXTERNALDB_PORT
            {{- toYaml .Values.primeagent.backend.externalDatabase.port | nindent 14 }}
            - name: LF_CHART_EXTERNALDB_DATABASE
            {{- toYaml .Values.primeagent.backend.externalDatabase.database | nindent 14 }}
            - name: LF_CHART_EXTERNALDB_USER
            {{- toYaml .Values.primeagent.backend.externalDatabase.user | nindent 14 }}
            - name: LF_CHART_EXTERNALDB_PASSWORD
            {{- toYaml .Values.primeagent.backend.externalDatabase.password | nindent 14 }}
            {{- end }}
            {{- if and (hasKey .Values.primeagent.backend "autoLogin") (eq .Values.primeagent.backend.autoLogin false) }}
            - name: PRIMEAGENT_AUTO_LOGIN
              value: "{{ .Values.primeagent.backend.autoLogin | default "False" }}"
            - name: PRIMEAGENT_SUPERUSER
              value: "{{ .Values.primeagent.backend.superuser | default "admin" }}"
            - name: PRIMEAGENT_SUPERUSER_PASSWORD
              value: "{{ .Values.primeagent.backend.superuserPassword | default (randAlphaNum 32) }}"
            - name: PRIMEAGENT_SECRET_KEY
              value: "{{ .Values.primeagent.backend.secretKey | default (randAlphaNum 32) }}"
            - name: PRIMEAGENT_NEW_USER_IS_ACTIVE
              value: "{{ .Values.primeagent.backend.newUserIsActive | default "False" }}"
            {{- end }}
          resources:
            {{- toYaml .Values.primeagent.backend.resources | nindent 12 }}
      {{- with .Values.primeagent.backend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.primeagent.backend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.primeagent.backend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if .Values.primeagent.backend.sqlite.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: {{ .Values.primeagent.backend.sqlite.volume.size }}
      {{- if .Values.primeagent.backend.sqlite.volume.existingStorageClassName }}
      {{- if ne .Values.primeagent.backend.sqlite.volume.existingStorageClassName "default"}}
      storageClassName: {{ .Values.primeagent.backend.sqlite.volume.existingStorageClassName }}
      {{- else }}
      {{- end }}
      {{- else }}
      storageClassName: "{{ template "primeagent.fullname" . }}-sqlite"
      {{- end }}
      
  {{- end }}

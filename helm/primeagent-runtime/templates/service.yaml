apiVersion: v1
kind: Service
metadata:
  name: {{ include "primeagent-runtime.fullname" . }}
  labels:
    {{- include "primeagent-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "primeagent-runtime.selectorLabels" . | nindent 4 }}
